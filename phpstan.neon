includes:
    - phpstan-baseline.neon

parameters:
    inferPrivatePropertyTypeFromConstructor: true
    tmpDir: .build/phpstan/
    paths:
        - src
        - tests
    level: max
    excludePaths:
        - tests/*/data/*